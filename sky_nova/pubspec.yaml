name: sky_nova
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get:
  firebase_core:
  cloud_firestore:
  firebase_auth:
  flutter_animate:
  intl:
  image_picker :
  firebase_storage:

flutter:
  assets:
    - assets/
